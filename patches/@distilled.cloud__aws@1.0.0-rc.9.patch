diff --git a/lib/endpoint.js b/lib/endpoint.js
index 4e0fc01f6adc9463772f0b5cf45a919b53ee4f47..d006012115d7eb055f61666d7e8262e6b0c0ac0d 100644
--- a/lib/endpoint.js
+++ b/lib/endpoint.js
@@ -11,7 +11,7 @@ export class Endpoint extends Context.Service()("AWS::Endpoint") {
  * Lambda containers). Resolves `undefined` when unset, so the SDK's
  * default endpoint resolver runs.
  */
-export const fromEnvironment = Config.string("AWS_ENDPOINT_URL").pipe(Config.option, Effect.map(Option.getOrUndefined));
+export const fromEnvironment = (Config.String ?? Config.string)("AWS_ENDPOINT_URL").pipe(Config.option, Effect.map(Option.getOrUndefined));
 /** Override the endpoint with whatever the environment names, if anything. */
 export const fromEnv = () => Layer.succeed(Endpoint, fromEnvironment.pipe(Effect.orDie));
 /** Override the endpoint for a scope, e.g. `Endpoint.of("http://localhost:4566")`. */
diff --git a/lib/region.js b/lib/region.js
index 83dbd72c93edd1a53faf4c75fbd325ae3174aab8..73e2dc222501cc155fe67de6fa0db7c3c466da2c 100644
--- a/lib/region.js
+++ b/lib/region.js
@@ -25,7 +25,7 @@ export class Region extends Context.Service()("AWS::Region") {
  * (rather than dying) so a provider can turn it into a typed
  * `MissingRegion` alongside its other credential-resolution failures.
  */
-export const fromEnvironment = Config.string("AWS_REGION").pipe(Config.orElse(() => Config.string("AWS_DEFAULT_REGION")), Effect.map((region) => region));
+export const fromEnvironment = (Config.String ?? Config.string)("AWS_REGION").pipe(Config.orElse(() => (Config.String ?? Config.string)("AWS_DEFAULT_REGION")), Effect.map((region) => region));
 /** Override the region with whatever the environment names. */
 export const fromEnv = () => Layer.succeed(Region, fromEnvironment.pipe(Effect.orDie));
 /** Override the region for a scope, e.g. `Region.of("us-west-2")`. */
diff --git a/src/endpoint.ts b/src/endpoint.ts
index 88ed29839b44007c4950dd558f2d039017962c0e..bf1ff2629d918f7b629b5d56b9fcc8534828751c 100644
--- a/src/endpoint.ts
+++ b/src/endpoint.ts
@@ -15,7 +15,7 @@ export class Endpoint extends Context.Service<
  * Lambda containers). Resolves `undefined` when unset, so the SDK's
  * default endpoint resolver runs.
  */
-export const fromEnvironment = Config.string("AWS_ENDPOINT_URL").pipe(
+export const fromEnvironment = (Config.String ?? Config.string)("AWS_ENDPOINT_URL").pipe(
   Config.option,
   Effect.map(Option.getOrUndefined),
 );
diff --git a/src/region.ts b/src/region.ts
index 6c5d77e3e6f4152d0207e288f9f998868c9968fe..a7597b2752287b4e07366813c3351422a6c94858 100644
--- a/src/region.ts
+++ b/src/region.ts
@@ -29,8 +29,8 @@ export class Region extends Context.Service<
  * (rather than dying) so a provider can turn it into a typed
  * `MissingRegion` alongside its other credential-resolution failures.
  */
-export const fromEnvironment = Config.string("AWS_REGION").pipe(
-  Config.orElse(() => Config.string("AWS_DEFAULT_REGION")),
+export const fromEnvironment = (Config.String ?? Config.string)("AWS_REGION").pipe(
+  Config.orElse(() => (Config.String ?? Config.string)("AWS_DEFAULT_REGION")),
   Effect.map((region) => region as RegionName),
 );
 
