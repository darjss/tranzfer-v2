# Copy this file to `.env` (Vite) and `.dev.vars` (workerd). Same keys.
# Deploy platforms provide the real values as secrets / Worker vars.

# Signs the session cookie. Comma-separated, newest first: prepend a fresh
# secret to rotate ("new,old"), drop the old one after a week.
# Generate one:  openssl rand -base64 32
SESSION_SECRET=
