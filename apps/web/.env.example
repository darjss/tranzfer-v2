# Public build-time values (VITE_ prefix). Optional; defaults to Tranzfer.
# VITE_APP_NAME=Tranzfer

# Copy needed keys to `.env` (Vite) and `.dev.vars` (workerd).
# Do not put Worker secrets or service bindings here.
